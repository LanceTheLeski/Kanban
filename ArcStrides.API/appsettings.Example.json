{
  "//": "Copy this file to appsettings.json and fill in your own connection string. appsettings.json is gitignored so the key never reaches the repo. Alternatively leave the placeholder and use: dotnet user-secrets set \"AzureTables:ServiceEndpoint\" \"<connection string>\" --project ArcStrides.API",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AzureTables": {
    "ServiceEndpoint": "DefaultEndpointsProtocol=https;AccountName=<account>;AccountKey=<key>;EndpointSuffix=core.windows.net"
  }
}
