@using Kanban.Contracts.Response
@using Kanban.UI.Components

<KanbanOverlay @bind-Open="Open">
    <MudText>@TaskData.Title</MudText>
    <MudText>Completed: @(TaskData.isCompleted)</MudText>
    <!--<MudExpansionPanels>
        <MudExpansionPanel Text="Column" MaxHeight="1000" ExpandedChanged="TaskTypeListExpandedChanged">
            @_taskTypeList
        </MudExpansionPanel>
    </MudExpansionPanels>

    <MudExpansionPanels>
        <MudExpansionPanel Text="Swimlane" MaxHeight="1000" ExpandedChanged="SwimlaneListExpandedChanged">
            @_swimlaneList
        </MudExpansionPanel>
    </MudExpansionPanels>-->
</KanbanOverlay>

<!--<MudOverlay @bind-Visible="_taskViewIsOpen" DarkBackground="true" ZIndex="20">
    <UpdateTaskOverlay @bind-Open="_taskViewIsOpen"
                       @bind-TaskData="_swimlanes"
                       Refresh="Refresh" />
</MudOverlay>-->

@code 
{
    [Parameter]
    public bool Open { get; set; }
    [Parameter]
    public EventCallback<bool> OpenChanged { get; set; }

    [Parameter]
    public TaskResponse TaskData { get; set; }
    [Parameter]
    public EventCallback<TaskResponse> TaskDataChanged { get; set; }

    [Parameter]
    public EventCallback<bool> Refresh { get; set; }

    private static bool _taskViewIsOpen = false;
    private static MonthResponse.BasicCard _taskToView = null;
}