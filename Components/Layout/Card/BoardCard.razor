@using Kanban.Components.DTOs
@using Kanban.Components.DTOs.ToPossiblyDelete;

<MudPaper Class="rounded-lg gap-0" Style="width: 100px; height: 200px; text-align: center; background-color: lightyellow">
    <MudStack Class="d-inline-flex flex-grow-1" AlignItems="AlignItems.Center" Spacing="0">
        <MudPaper Width="100%" MinHeight="160px;">
            <MudListItem OnClick="(() => OpenOverlay(Card))">

                <MudText Align="Align.Center">@Card.Title</MudText>
                <br />
                <MudText Style="max-height: 70px;">@Card.Description</MudText>

            </MudListItem>
        </MudPaper>
        <MudPaper Width="100%">
            <MudButton Size="Size.Small">Edit</MudButton>
        </MudPaper>
    </MudStack>
</MudPaper>

@code 
{
    [Parameter]
    public DropCard Card { get; set; }
    [Parameter]
    public EventCallback<DropCard> CardChanged { get; set; }

    public void OpenOverlay (DropCard cardToOverlay)
    {
        
    }
}